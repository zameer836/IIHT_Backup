function reverseArray(inputArray){
    var reversed = [];

    for(let i = inputArray.length-1; i>=0; i--) {
        reverseArray.push(inputArray[i]);
    return reversed;
}

var inputArray = [1,2,3,4,5];
let rev = new reverseArray(inputArray);
console.log(rev.reverseArray);
